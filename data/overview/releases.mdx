---
title: Releases
description: Styple releases and changelog.
---

### Initial release! 🎉

<Release name="@styple/design-system" version="v0.1.0" current />

- 40+ accessible components
- Default light and dark themes
- Open-source
- Powered by [Stitches](https://stitches.dev/) and [Radix-UI](https://www.radix-ui.com/)

<Release name="@styple/hooks" version="v0.1.0" current />

- useMounted
- useInterval
- useActiveElement
- useOnScreen
- useDebounce
- useCopy

```jsx outline=true
// Plain jsx codeblock
import React from 'react';

export function Counter({ initialCount = 0 }) {
  const [count, setCount] = React.useState(initialCount);
  return (
    <button type="button" onClick={() => setCount((prevCount) => prevCount + 1)}>
      {count}
    </button>
  );
}
```

```ts
import { useEffect, useState } from "__react__";

// Jsx codeblock with highlighted word
export const useMounted = () => {
  const [mounted, setMounted] = useState<boolean>(false);

  useEffect(() => {
    setMounted(true);
  }, []);

  return mounted;
};
```

```ts
import React from 'react';

// Jsx codeblock with highlighted word
export function useMounted() {
  const [mounted, setMounted] = React.useState<boolean>(false);

  React.useEffect(() => {
    setMounted(true);
  }, []);

  return mounted;
};
```

```html line=3
<div>
  <h1>Highlight line 3</h1>
  <p>This is a code block example.</p>
  <p>Do you like it?</p>
</div>
```

```html line=1-3
<div>
  <h1>Highlight line 1-3</h1>
  <p>This is a code block example.</p>
  <p>Do you like it?</p>
</div>
```

```html line=1,3
<div>
  <h1>Highlight line 1,3</h1>
  <p>This is a code block example.</p>
  <p>Do you like it?</p>
</div>
```

Hover highlight: <H id="demo1" index="1">hover me</H>.

```jsx id=demo1
import React from '__react__';

function Hello() {
  return Hello world;
}
```

<RegisterLink id="demo2" index="1" href="https://reactjs.org/" />

```jsx id=demo2 
import React from '__react__';

// React above should be link
function Hello() {
  return Hello world;
}
```

Test inline code `works?` maybe 

---
