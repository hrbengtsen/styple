---
title: SearchField
description: A basic text input field used as search.
---

### Example

<Preview>
  <Container css={{ px: "$md" }}>
    <Searchbar />
  </Container>
</Preview>

```tsx outline=true
import { CommandPalette } from "@styple/design-system";

const dummyData = [
  "Docs page 1",
  "Docs page 2",
  "Docs page 3",
  "Last docs page"
];

// Boilerplate component of how to implement a searchbar with SearchField
export const CommandPaletteContainer = () => {
  const [query, setQuery] = useState("");

  const filteredData = query === "" ? dummyData : dummyData.filter((item) => (item.toLowerCase().includes(query.toLowerCase())));

  return (
    <form style={{ width: "100%" }}>
      <SearchField
        query={query}
        setQuery={setQuery}
        keyPressed={keyPressed}
        placeholder="Search test.."
      />
    </form>
  );
};
```

### API Reference

#### SearchField

A styled component using `TextField` internally. Custom props:

<Table>
  <Thead>
    <Tr>
      <Th>Prop</Th>
      <Td>Type</Td>
      <Td>Default</Td>
      <Td>Explanation</Td>
    </Tr>
  </Thead>
  <Tbody>
    <Tr>
      <Th>
        <Code>query</Code>
      </Th>
      <Td>
        <Code color="normal">string</Code>
      </Td>
      <Td>-</Td>
      <Td><Text>The current text string.</Text></Td>
    </Tr>
    <Tr>
      <Th>
        <Code>setQuery</Code>
      </Th>
      <Td>
        <Code color="normal">SetStateAction</Code>
      </Td>
      <Td>-</Td>
      <Td><Text>React state setter called `onChange` with target value.</Text></Td>
    </Tr>
    <Tr>
      <Th>
        <Code>keyPressed</Code>
      </Th>
      <Td>
        <Code color="normal">KeyboardEventHandler</Code>
      </Td>
      <Td>-</Td>
      <Td><Text>Keyboard event called `onKeyPress` with event.</Text></Td>
    </Tr>
  </Tbody>
</Table>
